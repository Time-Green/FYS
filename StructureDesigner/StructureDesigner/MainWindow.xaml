<mah:MetroWindow x:Class="StructureDesigner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StructureDesigner"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        mc:Ignorable="d"
        Title="Structure Designer" Height="720" Width="1280">
    <Grid x:Name="MainGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="260px"/>
        </Grid.ColumnDefinitions>

        <Canvas x:Name="Canvas" Background="#2C2F33" Grid.Column="0" AllowDrop="True" Drop="Canvas_Drop"/>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="3*"/>
            </Grid.RowDefinitions>

            <ListView x:Name="LayerView" Grid.Row="0" Background="#23272A"/>
            <ScrollViewer Grid.Row="1" Background="#23272A">
                <WrapPanel x:Name="SelectionPanel" Height="auto"/>
            </ScrollViewer>
        
        </Grid>
    </Grid>
</mah:MetroWindow>
